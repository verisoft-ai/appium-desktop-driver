/** Locator strategy the CSS emitter targets — our existing UIA condition DSL. */
export const UIA_CONDITION_STRATEGY = '-windows uiautomation';

/** Registry key for the UIA condition CSS emitter. */
export const UIA_CONDITION_EMITTER_KEY = 'uiaCondition';
