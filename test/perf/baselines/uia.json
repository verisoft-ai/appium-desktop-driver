{
  "_comment": "Reference-machine p50 milliseconds keyed by <op>@<nodeCount>. checkRegressions() fails a run only when p50 exceeds 3x the value here; entries absent from this map are recorded but not gated.",
  "referenceMachine": "local dev — Intel Core 5 120U, 12 cores, 17GB, Windows 11 (2026-09-03, sha 05df564 + UIA per-level cache request). ~1744 nodes @ nodeCount=1500.",
  "baseline": {
    "getPageSource@1500": 4982,
    "findAll-star@1500": 5858,
    "find-anchorLast@1500": 5061,
    "getAttribute-x50@1500": 685
  }
}
